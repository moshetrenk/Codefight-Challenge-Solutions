String makeSentence(char[][] words) {
    String s = "";
    for(char[] a : words){
        for(char ch : a){
            s += ch;
        }
        s += " ";
    }
    s = s.substring(0, s.length() - 1);
    s+= ".";
    
    return s;
}
